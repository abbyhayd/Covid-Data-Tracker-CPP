	lli r1 0 # num = 0
	lli r2 0 # high = 0

do:	in r1		#user input
	sub r3 r1 r2	#dif btwn num and high
	bgt r3 &setter	#setter branch
	bge r1 &do	#loop
	

setter: lli r2 r1	#sets num to high if it's greater than
	bgt r0 &do	#back to loop


bne r2 &cond2		#checks if high is != 0

cond2:	blt r1 &then	#checks if num is 0

then:	out r2		#outputs highest num
	.halt

out r0			#reached if negative num is entered w/o other
			#input
